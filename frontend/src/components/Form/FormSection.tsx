import styled from '@emotion/styled';

const FormSection = styled.div`
  width: 100%;
  text-align: left;
  .label, 
  > label {
    line-height: 5em;
    font-weight: 700;
  }
`;

export default FormSection;

