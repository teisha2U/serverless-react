export default interface ICognitoConfig {
  cognito_state: string;
  cognito_client_id: string;
  cognito_domain: string;
  cognito_redirect_url: string;
  cognito_response_type: string;
  region: string;
}
